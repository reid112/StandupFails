ktor {
    deployment {
        port = 8880
        port = ${?PORT}
    }
    application {
        modules = [ ca.rjreid.ApplicationKt.module ]
    }
}

db {
    jdbcUrl = ${DB_URL}
    dbUser = ${DB_USER}
    dbPassword = ${DB_PASSWORD}
}
